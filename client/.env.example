# Optional. If omitted, the client will call the current host on port 4000.
VITE_API_URL=http://localhost:4000/api

# Optional. Cloudflare Web Analytics token (cookie-free pageview tracking)
# Get this from your Cloudflare dashboard and set it only in production.
VITE_CF_ANALYTICS_TOKEN=
